{
  "permissions": {
    "allow": [
      "Bash(pip install:*)",
      "Bash(npm install:*)",
      "Bash(python -c \"from main import app; from resolver import resolve; from models_config import MODELS; print\\(''Backend imports OK, models:'', list\\(MODELS.keys\\(\\)\\)\\)\")",
      "Bash(python -c \":*)"
    ]
  }
}
